<Mod>
	<Name>Timer Plus</Name>
	<Author>wizz6rd</Author>
	<Version>0.1.0</Version>

	<Description>
	Two blocks, each of which is up to thirty-two of Besiege's own.

	Timer Plus is up to thirty-two of Besiege's timer blocks. Each row of its
	table is a timer with the same settings the game's own has -- activation key
	or variable, hold to run, allow stop, loop, wait, emulation duration and the
	key or variable it presses -- and the rows can be sorted by any column. A
	button at the bottom converts the table into that many real timer blocks,
	laid out on one horizontal plane and handed over as a selection.

	Logic Gate Plus is the same table for Besiege's logic gates: two inputs, the
	gate, its one switch and the key it presses, a row apiece.

	Block models by Poly by Google and Robert Schlyter, from Poly Pizza, under
	CC-BY 3.0. The settings
	panel needs UI Factory (Workshop item 2913469777); without it the block falls
	back to Besiege's own block mapper.
	</Description>

	<!-- The node editor's own hotkeys. Declared here so Besiege puts them in its
	     controls screen and the player can rebind them like any other.

	     The modifier is the 1 key rather than Control on purpose: Besiege's own
	     copy, paste, undo and redo are Ctrl+C, V, Z and Y, the game gets the
	     keyboard at the same time this does, and a board paste that also
	     duplicated the block under it is not a paste anybody asked for. -->
	<Keys>
		<Key name="board-copy" defaultModifier="Alpha1" defaultTrigger="C" />
		<Key name="board-paste" defaultModifier="Alpha1" defaultTrigger="V" />
		<Key name="board-undo" defaultModifier="Alpha1" defaultTrigger="Z" />
		<Key name="board-redo" defaultModifier="Alpha1" defaultTrigger="Y" />
	</Keys>

	<Icon name="TimerPlus_icon" />
	<Debug>false</Debug>
	<MultiplayerCompatible>true</MultiplayerCompatible>

	<Assemblies>
		<Assembly path="TimerPlus.dll" />
	</Assemblies>

	<Blocks>
		<Block path="TimerPlus.xml" />
		<Block path="LogicGatePlus.xml" />
	</Blocks>

	<Resources>
		<Mesh name="TimerPlus_mesh" path="TimerPlus.obj" />
		<Texture name="TimerPlus_texture" path="TimerPlus.png" />
		<Mesh name="LogicGatePlus_mesh" path="LogicGatePlus.obj" />
		<Texture name="LogicGatePlus_texture" path="LogicGatePlus.png" />
		<Texture name="TimerPlus_icon" path="Thumbnail.png" />

		<!-- The table's three switch columns are headed with a picture rather
		     than a letter. Drawn by tools/make-ui-icons.py from the artwork in
		     tools/icons; loaded by name in Glyphs.cs. -->
		<Texture name="TimerPlus_hold" path="Hold.png" />
		<Texture name="TimerPlus_stop" path="Stop.png" />
		<Texture name="TimerPlus_loop" path="Loop.png" />

		<!-- The node editor's own two: the comment node's picture in its palette,
		     and the pin that holds the board open once the block's menu has gone.
		     Same pipeline as the three above. -->
		<Texture name="TimerPlus_bubble" path="Bubble.png" />
		<Texture name="TimerPlus_pin" path="Pin.png" />

		<!-- The node editor's drawn glyphs. Every one of them used to be drawn a
		     pixel at a time when the game first asked for it, which was a stall on
		     the first block opened; they are made by tools/make-glyphs.py now and
		     loaded by name in Glyphs.cs. -->
		<Texture name="TimerPlus_gate_not" path="glyph-not.png" />
		<Texture name="TimerPlus_gate_and" path="glyph-and.png" />
		<Texture name="TimerPlus_gate_or" path="glyph-or.png" />
		<Texture name="TimerPlus_gate_nor" path="glyph-nor.png" />
		<Texture name="TimerPlus_gate_nand" path="glyph-nand.png" />
		<Texture name="TimerPlus_gate_xor" path="glyph-xor.png" />
		<Texture name="TimerPlus_gate_xnor" path="glyph-xnor.png" />
		<Texture name="TimerPlus_gate_random" path="glyph-random.png" />
		<Texture name="TimerPlus_gate_srlatch" path="glyph-srlatch.png" />
		<Texture name="TimerPlus_gate_dlatch" path="glyph-dlatch.png" />
		<Texture name="TimerPlus_gate_counter" path="glyph-counter.png" />
		<Texture name="TimerPlus_gate_edge" path="glyph-edge.png" />
		<Texture name="TimerPlus_ring" path="glyph-ring.png" />
		<Texture name="TimerPlus_dot" path="glyph-dot.png" />
		<Texture name="TimerPlus_plate" path="glyph-plate.png" />
		<Texture name="TimerPlus_arrow" path="glyph-arrow.png" />
		<Texture name="TimerPlus_resize" path="glyph-resize.png" />
		<Texture name="TimerPlus_resize2" path="glyph-resize2.png" />
		<Texture name="TimerPlus_grid" path="glyph-grid.png" />
		<Texture name="TimerPlus_dash" path="glyph-dash.png" />
		<Texture name="TimerPlus_dashdown" path="glyph-dashdown.png" />
		<Texture name="TimerPlus_bars" path="glyph-bars.png" />
	</Resources>

<!-- This value is automatically generated. Do not change it or you may break machine&level save files. -->


<!-- This value is automatically generated. Do not change it or you may break machine&level save files. -->
<ID>5e4cf293-3cb4-494d-8009-fce53ade8e4a</ID>
</Mod>